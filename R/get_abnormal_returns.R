GetAbnormalReturns <- function(companies_rates, index_rates,
                               model =
                                   c("mean_adjusted", "market_adjusted", "OLS"),
                               estimation_start, estimation_end,
                               event_start, event_end) {

}
