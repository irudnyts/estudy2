check_event_cumulatively <- function(..., index, model,
                                              estimation_start,
                                              estimation_end,
                                              event_start, event_end,
                                              quote = c("Close", "Open"),
                                              critical_percentage,
                                              start, end,
                                              only_significant = T,
                                              whole_event_window = F) {

}
