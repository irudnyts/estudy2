complete_event_study <- function(company_tickers, index_ticker,
                                 quote = c("Open", "Close"),
                                 compounding = c("periodic", "continuous"),
                                 dividends = c(F, T),
                                 market_model = c("SIM"),
                                 estimation_method = c("OLS"),
                                 estimation_start, estimation_end,
                                 event_start, event_end,
                                 car_start, car_end) {
    # get_prices_from_tickers()
    # get_rates_from_prices()
    # apply_market_model()
    # parametric_tests()
    # nonparametric_tests()
    # car_parametric_tests()
    # car_nonparametric_tests()
}
