check_event_cumulatively_parallel <- function(..., index, model,
                                              estimation_start,
                                              estimation_end,
                                              event_start, event_end,
                                              quote = c("Close", "Open"),
                                              critical_percentage,
                                              os = c("mac", "win"),
                                              start, end,
                                              only_significant = T,
                                              whole_event_window = F) {

}
